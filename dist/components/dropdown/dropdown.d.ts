import { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    content: ReactNode;
};
export declare const Dropdown: ({ children, content }: Props) => JSX.Element;
export {};
