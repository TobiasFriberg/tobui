import React from 'react';
type Props = {
    title: string;
    children: Object;
    expanded?: boolean;
    className?: string;
};
export declare const Expander: ({ title, children, expanded, className }: Props) => React.JSX.Element;
export {};
