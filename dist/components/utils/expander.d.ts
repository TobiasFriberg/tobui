/// <reference types="react" />
declare type Props = {
    title: string;
    children: Object;
    expanded?: boolean;
};
export declare const Expander: ({ title, children, expanded }: Props) => JSX.Element;
export {};
