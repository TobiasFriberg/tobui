/// <reference types="react" />
declare type Props = {
    size?: string;
    light?: boolean;
    fillPage?: boolean;
    testId?: string;
};
export declare const Loader: ({ size, light, fillPage, testId }: Props) => JSX.Element;
export {};
