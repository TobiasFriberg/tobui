import { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    value: string | number;
};
export declare const Badge: ({ children, value }: Props) => JSX.Element | null;
export {};
